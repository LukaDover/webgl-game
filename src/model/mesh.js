export class Mesh {
    constructor(vertexPositions, vertexTextures, vertexNormals, indices) {
        this.vertexPositions = vertexPositions;
        this.vertexTextures = vertexTextures;
        this.vertexNormals = vertexNormals;
        this.indices = indices;
    }
}